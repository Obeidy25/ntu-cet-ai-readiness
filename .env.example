# هذا الملف نموذج (template) بس - انسخه لـ .env وعبّي القيم الحقيقية
# .env نفسه ما ينرفع أبداً لـ GitHub (لازم يكون بـ .gitignore)

# اختياري: بس إذا تريد تختبر مسار OpenAI مباشرة من السيرفر (نادراً ما نحتاجه
# لأن المفتاح هسه يدخله المستخدم بالواجهة مباشرة). خليه فاضي إذا ما تحتاجه.
OPENAI_API_KEY=
